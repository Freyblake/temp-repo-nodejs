const num1 = 20;
const num2 = 94;

function addValue() {
  console.log(`sum is : ${num1 + num2}`);
}

addValue();

// here the function is invoked so when we {use 'require'} require that module than in our app.js file then this function is automatically invoked and whatever the function returns or do anything it will give us that output
